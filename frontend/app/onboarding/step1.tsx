import { Step1Screen } from './_screens/Step1Screen';

/**
 * Route wrapper for Step 1
 * Expo Router requires files at route level
 */
export default function Step1Route() {
  return <Step1Screen />;
}
