# File-global directive (its position does not scope it): adds PyTorch's own
# wheel index alongside PyPI, which is where the +cpu builds below live.
--extra-index-url https://download.pytorch.org/whl/cpu

annotated-doc==0.0.4

# torch arrives as a transitive dependency of sentence-transformers. Left
# unpinned, pip resolves PyPI's default Linux wheel, which bundles the NVIDIA
# CUDA runtime (~2.5GB) — dead weight on Railway and CI, both CPU-only. The
# "+cpu" local version identifier exists only on PyTorch's index, so pinning it
# forces the right wheel deterministically. macOS publishes no +cpu wheel (and
# has no CUDA build to avoid), hence the environment marker split.
# NOTE: this pin means the app cannot use a GPU even where one is available.
torch==2.11.0+cpu ; sys_platform == "linux"
torch==2.11.0 ; sys_platform != "linux"
sentence-transformers==5.4.0
psycopg2-binary
pgvector
annotated-types==0.7.0
anyio==4.12.1
asyncpg==0.31.0
CacheControl==0.14.4
certifi==2026.5.20
cffi==2.0.0
charset-normalizer==3.4.5
click==8.4.1
cryptography==49.0.0
dnspython==2.8.0
email-validator==2.3.0
fastapi==0.135.1
firebase_admin==7.4.0
google-api-core==2.31.0
google-auth==2.49.0
google-cloud-core==2.6.0
google-cloud-firestore==2.27.0
google-cloud-storage==3.9.0
google-crc32c==1.8.0
google-resumable-media==2.8.0
googleapis-common-protos==1.73.0
greenlet==3.3.2
grpcio==1.78.0
grpcio-status==1.78.0
h11==0.16.0
h2==4.3.0
hpack==4.1.0
httpcore==1.0.9
httptools==0.7.1
httpx==0.28.1
hyperframe==6.1.0
idna==3.15
msgpack==1.1.2
proto-plus==1.27.1
protobuf==6.33.5
pyasn1==0.6.3
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.12.5
pydantic-settings==2.14.1
pydantic_core==2.41.5
PyJWT==2.12.0
python-dotenv==1.2.2
PyYAML==6.0.3
requests==2.34.2
rsa==4.9.1
SQLAlchemy==2.0.48
starlette==0.52.1
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2025.2
urllib3==2.7.0
uvicorn==0.41.0
uvloop==0.22.1
watchfiles==1.1.1
websockets==16.0
pytest==9.0.3
pytest-asyncio==1.3.0
